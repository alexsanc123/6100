FROM debian:buster

RUN useradd -d /home/catsoop -m catsoop

RUN apt -y update
RUN apt -y dist-upgrade
RUN apt -y install python3-virtualenv python3 python3-pip uwsgi-plugin-python3 build-essential uwsgi git
RUN pip3 install black
